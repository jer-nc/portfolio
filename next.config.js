/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'pbs.twimg.com'], // Agrega aquí el dominio de tus imágenes
    },
}

module.exports = nextConfig
